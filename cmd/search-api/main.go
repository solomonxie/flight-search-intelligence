// Command search-api serves flexible, low-latency flight search (route,
// date range, airline, price, stops...) against the database
// cmd/collector populates.
package main

import "fmt"

func main() {
	// TODO: serve a search endpoint backed by internal/db.
	fmt.Println("flight-search-intelligence: search-api (scaffold, not yet implemented)")
}
