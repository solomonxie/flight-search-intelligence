# One group per purpose this Ansible setup manages. mac_dev is this
# developer's own Mac, provisioned locally; future groups (e.g. a
# [cicd] runner) get their own inventory group and playbook the same
# way, not folded into this one.

[mac_dev]
localhost ansible_connection=local
