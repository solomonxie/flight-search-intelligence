apiVersion: v1
kind: Service
metadata:
  name: search-api
spec:
  selector:
    app.kubernetes.io/name: search-api
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
